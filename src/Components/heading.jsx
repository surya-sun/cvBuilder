import React from "react";

let Heading = (props)=>{
    return(
        <>
                    <p className="h3 text-center mt-2 fw-bold">{props.heading}</p>
        </>
    )
}

export default Heading;